class ListItem {
  int value;
  String name;
  ListItem(this.value, this.name);
}

class ListItemforOrder {
  int value;
  String name;
  bool isSelected;
  ListItemforOrder(this.value, this.name, this.isSelected);
}
